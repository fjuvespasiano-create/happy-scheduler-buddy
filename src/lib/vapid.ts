// VAPID public key is now fetched at runtime from the server (auto-generated
// and stored in the database). Use `getVapidPublicKey` server function via
// `ensurePushSubscription` in src/lib/pwa.ts.
export {};
